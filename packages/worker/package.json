{
  "name": "@workflow/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@workflow/database": "workspace:*",
    "@workflow/engine": "workspace:*",
    "@workflow/queue": "workspace:*",
    "@workflow/shared": "workspace:*",
    "@workflow/tools": "workspace:*",
    "bullmq": "^5.34.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
